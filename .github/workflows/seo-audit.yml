name: SEO Audit

on:
  pull_request:
    paths:
      - 'apps/ratewise/**'
      - 'scripts/**'
      - '.github/workflows/seo-audit.yml'
  push:
    branches:
      - main
      - 'claude/**'

permissions:
  contents: read
  pull-requests: write

jobs:
  seo-audit:
    name: SEO 2026 Standards Audit
    runs-on: ubuntu-latest
    timeout-minutes: 15
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'
      # 使用本地檔案驗證 precache 資產，避免 CI 環境與生產環境資產 hash 不同導致 404
      VERIFY_PRECACHE_SOURCE: 'local'

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7

      - name: Setup pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10
        with:
          version: 9.10.0

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build RateWise
        run: pnpm build:ratewise

      - name: Run SEO Full Audit
        run: |
          chmod +x scripts/seo-full-audit.mjs
          node scripts/seo-full-audit.mjs

      - name: Upload audit report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: seo-audit-report
          path: |
            apps/ratewise/dist/
            apps/ratewise/public/sitemap.xml
          retention-days: 7

      - name: Comment PR (on failure)
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '❌ SEO 審計失敗！請檢查以下項目：\n\n' +
                    '- Sitemap 2026 標準合規性\n' +
                    '- Breadcrumb Schema 正確性\n' +
                    '- JSON-LD 結構化數據完整性\n\n' +
                    '詳細資訊請查看 [Actions 日誌](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})'
            })

      - name: Comment PR (on success)
        if: success() && github.event_name == 'pull_request'
        uses: actions/github-script@3a2844b7e9c422d3c10d287c895573f7108da1b3 # v9
        with:
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: '✅ SEO 審計通過！所有 2026 標準驗證項目都符合要求。\n\n' +
                    '- ✅ Sitemap 2026 標準\n' +
                    '- ✅ Breadcrumb Schema\n' +
                    '- ✅ JSON-LD 結構化數據\n' +
                    '- ✅ 內部連結結構'
            })
