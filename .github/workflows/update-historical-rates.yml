# 歷史匯率快照策略
#
# 職責：每日 0:00 將 latest.json 複製到 history/YYYY-MM-DD.json
#
# 特點：
# ✅ 低頻更新（每日一次）
# ✅ 單一職責（只保存歷史快照）
# ✅ 永久保存（不清理舊數據）
# ✅ 上游管線存活守門（持續 outage 時拒寫壞快照）
# ✅ 開源專案 GitHub Actions 完全免費

name: Update Historical Exchange Rates

# 郵件通知策略：只在失敗時通知
# GitHub Settings > Notifications > Actions > "Send notifications for failed workflows only"

# 並發控制：確保同時只有一個 workflow 能推送到 data 分支
concurrency:
  group: data-branch-push
  cancel-in-progress: false

on:
  schedule:
    # 每日 0:00 UTC (台北時間 8:00)
    - cron: '0 0 * * *'
    # 備援：GitHub scheduled run 偶發掉單（2026-07-04 實例）；冪等設計下重跑無副作用
    - cron: '30 1 * * *'

  # 允許手動觸發
  workflow_dispatch:

  # 測試用：當工作流被修改時執行
  # 資料更新 workflow 具有 data branch 寫入權限，push 事件只允許已審查的 main。
  push:
    branches:
      - main
    paths:
      - '.github/workflows/update-historical-rates.yml'

jobs:
  update-history:
    runs-on: ubuntu-latest

    permissions:
      contents: write
      # 讀取 update-latest-rates.yml 的 run 結果作為上游管線存活訊號。
      actions: read

    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

    steps:
      - name: Checkout data branch
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: data
          fetch-depth: 0

      - name: Checkout history generator from the triggering code SHA
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          ref: ${{ github.sha }}
          path: _fx-code
          persist-credentials: false

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7
        with:
          node-version: '24'

      # 上游管線存活守門：取代舊的 72h 資料齡檢查。
      # 資料齡不可靠——fetch 腳本只在 rates 變化時重寫 latest.json，週末/春節休市時
      # timestamp 會凍結多天（春節 6-9 天連假會讓資料齡守門誤報並拒寫正確快照）。
      # 改判斷「update-latest-rates.yml 最近 12 小時是否至少一次 success」：
      # - 有 success → 管線活著（正常更新或休市無變化）→ 照常寫快照
      # - 全部失敗/取消 → 持續 outage（如 2026-06-29 bot challenge 事故連寫 4 天壞資料）→ 拒寫
      # - 查詢失敗/窗口無 run → 告警但照常寫（告警責任在 update-latest 的 6h staleness gate；
      #   快照寧可保存最後已知良好資料，也不因 API 抖動中斷）
      # 耦合防護：本訊號依賴 update-latest-rates.yml 的 Data staleness gate——fetch step 雖
      # continue-on-error，持續 outage 超過 6h 後該 gate 會讓 run 轉紅，12h 窗口內才會出現
      # 零 success；最壞情況只會抄到 ~13h 舊資料（前一營業日收盤，語意可接受）。
      # 若移除或修改該 gate，必須同步重新設計本守門。
      - name: Upstream pipeline liveness gate
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          LIVENESS_WINDOW_HOURS: '12'
        run: |
          WINDOW_START=$(date -u -d "${LIVENESS_WINDOW_HOURS} hours ago" +%Y-%m-%dT%H:%M:%SZ)
          echo "🔎 Checking update-latest-rates.yml runs since ${WINDOW_START}..."

          if ! RUNS_JSON=$(gh run list \
            --workflow update-latest-rates.yml \
            --created ">=${WINDOW_START}" \
            --limit 200 \
            --json conclusion 2>/dev/null); then
            echo "::warning::Liveness query failed; proceeding with snapshot (alerting is owned by update-latest staleness gate)."
            exit 0
          fi

          TOTAL_RUNS=$(echo "$RUNS_JSON" | jq 'length')
          SUCCESS_RUNS=$(echo "$RUNS_JSON" | jq '[.[] | select(.conclusion == "success")] | length')
          echo "📊 Upstream runs in window: ${TOTAL_RUNS} total, ${SUCCESS_RUNS} success"

          if [ "$TOTAL_RUNS" -eq 0 ]; then
            echo "::warning::No update-latest-rates.yml runs found in the last ${LIVENESS_WINDOW_HOURS}h; proceeding with snapshot (alerting is owned by update-latest staleness gate)."
            exit 0
          fi

          if [ "$SUCCESS_RUNS" -eq 0 ]; then
            echo "❌ Persistent upstream outage: 0/${TOTAL_RUNS} successful runs in the last ${LIVENESS_WINDOW_HOURS}h."
            echo "::error::Historical snapshot aborted: rates pipeline has zero successful runs in ${LIVENESS_WINDOW_HOURS}h. Fix the pipeline, then backfill from https://rate.bot.com.tw/xrt/flcsv/0/<date>."
            exit 1
          fi

          echo "✅ Upstream pipeline alive (${SUCCESS_RUNS} successful runs) — safe to snapshot."

      - name: Save historical snapshot
        id: save-snapshot
        run: |
          # 建立 history 目錄
          mkdir -p public/rates/history

          # 取得當前日期（台北時區）
          CURRENT_DATE=$(TZ=Asia/Taipei date +%Y-%m-%d)
          HISTORY_FILE="public/rates/history/${CURRENT_DATE}.json"

          # 檢查 latest.json 是否存在
          if [ ! -f "public/rates/latest.json" ]; then
            echo "❌ Error: latest.json not found"
            exit 1
          fi

          # 檢查今天的歷史檔案是否存在
          if [ -f "$HISTORY_FILE" ]; then
            echo "✅ Today's snapshot already exists: ${HISTORY_FILE}"
            echo "📊 Checking if update is needed..."
            
            # 比較檔案內容
            if cmp -s "public/rates/latest.json" "$HISTORY_FILE"; then
              echo "✅ Snapshot is up to date"
              echo "snapshot_created=false" >> $GITHUB_OUTPUT
            else
              echo "♻️ Updating existing snapshot with latest rates"
              cp public/rates/latest.json "$HISTORY_FILE"
              echo "snapshot_created=true" >> $GITHUB_OUTPUT
            fi
          else
            echo "📅 Creating daily snapshot: ${HISTORY_FILE}"
            cp public/rates/latest.json "$HISTORY_FILE"
            echo "snapshot_created=true" >> $GITHUB_OUTPUT
          fi

          # 統計歷史檔案數量
          HISTORY_COUNT=$(ls -1 public/rates/history/*.json 2>/dev/null | wc -l)
          echo "history_count=${HISTORY_COUNT}" >> $GITHUB_OUTPUT
          echo "current_date=${CURRENT_DATE}" >> $GITHUB_OUTPUT

          echo "💾 Permanent storage: ${HISTORY_COUNT} days preserved"

      - name: Generate aggregate 30-day history file
        if: steps.save-snapshot.outputs.snapshot_created == 'true'
        id: generate-aggregate
        run: |
          RATE_DATA_ROOT=. node _fx-code/apps/ratewise/scripts/generate-history-aggregate.mjs
        env:
          RATE_DATA_ROOT: .

      - name: Aggregate file status
        if: steps.save-snapshot.outputs.snapshot_created == 'true'
        run: |
          echo "🧾 history-30d.json size:"
          ls -lh public/rates/history-30d.json

      - name: Commit and push snapshot
        if: steps.save-snapshot.outputs.snapshot_created == 'true'
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add public/rates/history/ public/rates/history-30d.json

          # 取得快照資訊
          CURRENT_DATE="${{ steps.save-snapshot.outputs.current_date }}"
          HISTORY_COUNT="${{ steps.save-snapshot.outputs.history_count }}"
          UPDATE_TIME=$(node -p "JSON.parse(require('fs').readFileSync('public/rates/latest.json')).updateTime")

          echo "📝 Creating snapshot commit..."
          git commit -m "chore(rates): daily snapshot - ${CURRENT_DATE}" \
            -m "Saved exchange rates snapshot for ${CURRENT_DATE}" \
            -m "📊 Total history: ${HISTORY_COUNT} days (permanent storage)" \
            -m "⏰ Source data timestamp: ${UPDATE_TIME}" \
            -m "🤖 Auto-updated daily at 00:00 UTC"

          # 推送前先拉取最新變更，避免並發衝突
          echo "🔄 Pulling latest changes before push..."
          git pull --rebase origin data || true

          # 重試機制：最多重試 3 次
          for i in 1 2 3; do
            echo "📤 Attempting to push (try $i/3)..."
            if git push origin data; then
              echo "✅ Push successful"
              break
            else
              if [ $i -lt 3 ]; then
                echo "⚠️ Push failed, retrying in 5 seconds..."
                sleep 5
                git pull --rebase origin data || true
              else
                echo "❌ Push failed after 3 attempts"
                exit 1
              fi
            fi
          done

      - name: Update workflow summary
        if: steps.save-snapshot.outputs.snapshot_created == 'true'
        run: |
          CURRENT_DATE="${{ steps.save-snapshot.outputs.current_date }}"
          HISTORY_COUNT="${{ steps.save-snapshot.outputs.history_count }}"

          echo "# ✅ Historical snapshot saved" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Snapshot Information" >> $GITHUB_STEP_SUMMARY
          echo "- **Date:** ${CURRENT_DATE}" >> $GITHUB_STEP_SUMMARY
          echo "- **Total History:** ${HISTORY_COUNT} days" >> $GITHUB_STEP_SUMMARY
          echo "- **Storage:** Permanent (no cleanup)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🔗 CDN URL" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "https://cdn.jsdelivr.net/gh/haotool/app@data/public/rates/history/${CURRENT_DATE}.json" >> $GITHUB_STEP_SUMMARY
          echo "\`\`\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📈 Sample Rates (TWD)" >> $GITHUB_STEP_SUMMARY
          echo "- **USD:** $(node -p "JSON.parse(require('fs').readFileSync('public/rates/latest.json')).rates.USD")" >> $GITHUB_STEP_SUMMARY
          echo "- **EUR:** $(node -p "JSON.parse(require('fs').readFileSync('public/rates/latest.json')).rates.EUR")" >> $GITHUB_STEP_SUMMARY
          echo "- **JPY:** $(node -p "JSON.parse(require('fs').readFileSync('public/rates/latest.json')).rates.JPY")" >> $GITHUB_STEP_SUMMARY
          echo "- **CNY:** $(node -p "JSON.parse(require('fs').readFileSync('public/rates/latest.json')).rates.CNY")" >> $GITHUB_STEP_SUMMARY

      - name: No snapshot needed
        if: steps.save-snapshot.outputs.snapshot_created != 'true'
        run: |
          CURRENT_DATE="${{ steps.save-snapshot.outputs.current_date }}"

          echo "# ℹ️ Snapshot already up to date" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Today's snapshot (${CURRENT_DATE}) is already saved and up to date." >> $GITHUB_STEP_SUMMARY
          echo "Next snapshot: Tomorrow at 00:00 UTC" >> $GITHUB_STEP_SUMMARY
