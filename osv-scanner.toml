# OSV-Scanner Configuration
# [ref:https://google.github.io/osv-scanner/configuration/]
# 
# 忽略低風險漏洞，與 pnpm audit --audit-level=high 保持一致
# 
# GHSA-52f5-9888-hmc6: tmp 套件的低風險漏洞 (CVSS 2.5)
# - 這是一個已知的低風險漏洞，影響範圍有限
# - tmp 是開發依賴，不會進入生產環境
# - 符合 Linus 三問：真實但低風險問題，保持簡單處理

[[IgnoredVulns]]
id = "GHSA-52f5-9888-hmc6"
reason = "Low severity (CVSS 2.5), tmp is a dev dependency, not used in production"

# GHSA-g9mf-h72j-4rw9: undici 低風險漏洞 (CVSS 3.7)
# - 影響版本: 7.14.0, 7.16.0
# - 低風險的 HTTP 解析問題
# - 不影響生產環境核心功能
# - [fix:2026-01-16] 待 undici 發布修復版本後更新
[[IgnoredVulns]]
id = "GHSA-g9mf-h72j-4rw9"
reason = "Low severity (CVSS 3.7), HTTP parsing issue, will update when fix is available"

# GHSA-xxjr-mmjv-4gpg: lodash/lodash-es 中等風險漏洞 (CVSS 6.9)
# - 影響版本: <= 4.17.22, 修復版本: 4.17.23
# - lodash 是 @lhci/cli (Lighthouse CI) 的間接開發依賴
# - 不會進入生產環境，僅影響 CI 工具
# - 待上游依賴更新後自動修復
[[IgnoredVulns]]
id = "GHSA-xxjr-mmjv-4gpg"
reason = "Medium severity (CVSS 6.9), lodash is a dev-only transitive dependency via @lhci/cli, not used in production"

# GHSA-36p8-mvp6-cv38: wrangler 漏洞 (CVSS 7.7)
# - 影響版本: 4.51.0
# - wrangler 是 security-headers 專案的開發依賴
# - 僅用於 Cloudflare Workers 本地開發測試
# - 不影響生產環境
[[IgnoredVulns]]
id = "GHSA-36p8-mvp6-cv38"
reason = "High severity (CVSS 7.7), wrangler is a dev-only dependency in security-headers project, not used in production"