# Build-time generated artifacts
# 由 prebuild 腳本自動產生，不應 commit 至主支
# 部署時 CI `pnpm build` 會重新生成，無需追蹤

# 每次 build 從 CDN 抓取的匯率快照
public/rates.json

# 由 git commit 日期驅動，每次 commit 後都會漂移
public/sitemap.xml

# 注意：robots.txt 由 seo-best-practices.test.ts 直接讀取（無 existsSync 保護），
# 必須 commit 至 repo 讓 CI 測試可通過。移除日期後內容穩定，不會造成 git 污染。
