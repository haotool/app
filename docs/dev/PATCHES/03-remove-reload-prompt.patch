# Patch 3: 刪除未使用的 ReloadPrompt 元件

## 問題

- `ReloadPrompt.tsx` 已在 `App.tsx` 中註解不使用
- 測試覆蓋率 0%
- PWA 自動更新由 `vite-plugin-pwa` 自動處理

## 刪除檔案

```bash
cd apps/ratewise
git rm src/components/ReloadPrompt.tsx
git rm src/components/ReloadPrompt.css
```

## 檔案: apps/ratewise/src/App.tsx

### Before

```typescript
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import { ErrorBoundary } from './components/ErrorBoundary';
// ReloadPrompt 不需要 - autoUpdate 自動註冊並更新  # ❌ 無效註解
import { SEOHelmet } from './components/SEOHelmet';
import CurrencyConverter from './features/ratewise/RateWise';

function App() {
  return (
    <ErrorBoundary>
      <SEOHelmet />
      <Router>
        <Routes>
          <Route path="/" element={<CurrencyConverter />} />
        </Routes>
      </Router>
    </ErrorBoundary>
  );
}

export default App;
```

### After

```typescript
import { BrowserRouter as Router, Routes, Route } from 'react-router-dom';
import { ErrorBoundary } from './components/ErrorBoundary';
import { SEOHelmet } from './components/SEOHelmet';
import CurrencyConverter from './features/ratewise/RateWise';

// PWA 自動更新由 vite-plugin-pwa 處理 (workbox autoUpdate)

function App() {
  return (
    <ErrorBoundary>
      <SEOHelmet />
      <Router>
        <Routes>
          <Route path="/" element={<CurrencyConverter />} />
        </Routes>
      </Router>
    </ErrorBoundary>
  );
}

export default App;
```

## 驗證

```bash
# 1. 確認檔案已刪除
ls -la src/components/ReloadPrompt.*
# 應顯示 "No such file"

# 2. 執行測試
pnpm test

# 3. 建置確認
pnpm build

# 4. 手動測試 PWA 更新
pnpm preview
# 開啟瀏覽器，檢查 SW 註冊與自動更新
```

## Commit 訊息

```
chore: remove unused ReloadPrompt component

- ReloadPrompt.tsx 已被 vite-plugin-pwa autoUpdate 取代
- 測試覆蓋率 0%，無實際使用
- PWA 自動更新機制正常運作

Closes #<issue-number>
```

## 相關 Issue

- TECH_DEBT_AUDIT.md § 風險 #8
- REFACTOR_PLAN.md § M0.2

