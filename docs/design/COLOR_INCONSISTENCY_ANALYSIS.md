# RateWise 色彩系統不一致分析報告（SSOT 建立前）

**分析時間點**: commit d069536 (f50fddb~1)
**分析範圍**: RateWise 主要組件的色彩使用情況
**目的**: 記錄 Design Token SSOT 系統建立前的色彩混亂狀況

---

## 📊 第一部分：各組件色彩清單

### 1.1 RateWise.tsx（主頁面）

**背景色系**:
- `bg-red-50` - 錯誤狀態背景
- `bg-red-500`, `hover:bg-red-600` - 錯誤按鈕
- `bg-white` - 卡片背景
- `bg-gradient-to-r from-blue-50 to-purple-50` - 模式切換器背景
- `bg-gradient-to-br from-blue-50 via-indigo-50 to-purple-50` - 主容器背景
- `bg-indigo-100` - FAQ 圖標背景
- `bg-gradient-to-r from-slate-50 to-white` - FAQ 項目背景
- `bg-gradient-to-br from-slate-900 via-blue-900 to-indigo-900` - Footer 背景

**文字色系**:
- `text-red-500` - 錯誤圖標
- `text-gray-800` - 標題
- `text-gray-600` - 副標題、說明文字
- `text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600` - 品牌標題
- `text-indigo-600`, `hover:text-indigo-700` - FAQ 連結
- `text-slate-800` - FAQ 標題
- `text-slate-600` - FAQ 內容
- `text-slate-500` - FAQ 摘要

**邊框色系**:
- `border-blue-100/50` - 模式切換器邊框
- `border-slate-100` - FAQ 項目邊框

**不一致點**:
1. 灰色系混用：`gray-600`, `gray-800`, `slate-500`, `slate-600`, `slate-800`
2. 藍色系混用：`blue-50`, `blue-600`, `indigo-50`, `indigo-100`, `indigo-600`
3. Footer 使用 `slate-900`, `blue-900`, `indigo-900` 三種深色

---

### 1.2 SingleConverter.tsx（單幣別轉換）

**背景色系**:
- `bg-gray-100` - 貨幣選擇器背景
- `hover:bg-blue-100`, `active:bg-blue-200` - 快速金額按鈕
- `bg-gradient-to-r from-blue-100 to-purple-100` - 匯率卡片背景
- `bg-gradient-to-r from-blue-50/95 to-purple-50/95` - 匯率類型選擇器
- `bg-gradient-to-r from-blue-500 to-purple-500` - 交換按鈕
- `hover:from-blue-600 hover:to-purple-600` - 交換按鈕 hover
- `bg-purple-50` - 結果輸入框背景
- `bg-purple-100`, `hover:bg-purple-200`, `active:bg-purple-300` - 結果快速金額按鈕
- `hover:bg-violet-50` - 計算機按鈕 hover

**文字色系**:
- `text-gray-700` - 標籤
- `text-gray-600` - 次要文字
- `text-violet-600`, `hover:text-violet-700` - 計算機圖標
- `text-transparent bg-clip-text bg-gradient-to-r from-blue-600 to-purple-600` - 主匯率
- `text-slate-600` - 次要資訊
- `text-red-500` - 錯誤訊息
- `text-purple-600`, `hover:text-purple-700` - 結果計算機圖標
- `text-purple-700`, `hover:text-purple-800` - Tooltip 文字（推測）

**邊框色系**:
- `border-gray-200` - 輸入框邊框
- `focus:border-blue-500`, `focus:ring-blue-500` - 來源輸入框 focus
- `border-purple-200` - 結果輸入框邊框
- `focus:border-purple-500`, `focus:ring-purple-500` - 結果輸入框 focus

**按鈕色系**:
- `bg-gradient-to-r from-blue-600 to-purple-600` - 主按鈕
- `hover:from-blue-700 hover:to-purple-700` - 主按鈕 hover

**不一致點**:
1. **灰色命名混亂**: `gray-100`, `gray-200`, `gray-600`, `gray-700` vs `slate-600`
2. **藍色深淺不一**: `blue-50`, `blue-100`, `blue-500`, `blue-600`, `blue-700`
3. **紫色系混用**: `purple-50` vs `violet-50`, `purple-600` vs `violet-600`
4. **紫色深淺跳躍**: `purple-100`, `purple-200`, `purple-300`, `purple-500`, `purple-600`, `purple-700`

---

### 1.3 MultiConverter.tsx（多幣別轉換）

**背景色系**:
- `bg-gray-100`, `hover:bg-blue-100` - 快速金額按鈕
- `bg-transparent` - 輸入框（無背景）

**文字色系**:
- `text-gray-700` - 標籤
- `text-gray-500` - 副標題、提示文字
- `text-gray-800` - 貨幣代碼
- `text-gray-600` - 貨幣名稱
- `text-yellow-500` - 收藏星號（已收藏）
- `text-gray-300` - 收藏星號（未收藏）
- `text-blue-600` - 匯率類型文字（可能）

**梯度色**:
- `from-yellow-50 to-amber-50` - 收藏項目背景（推測）
- `from-blue-50 to-purple-50` - 一般項目背景（推測）

**不一致點**:
1. **灰色階梯完整但命名不一致**: `gray-300`, `gray-500`, `gray-600`, `gray-700`, `gray-800`
2. **收藏項目顏色**:
   - 黃色系：`yellow-50`, `yellow-500`
   - 琥珀色系：`amber-50`
   - 藍紫梯度：`blue-50 to purple-50`

---

### 1.4 ConversionHistory.tsx（轉換歷史）

**背景色系**:
- `bg-white` - 卡片背景
- `bg-gray-50` - 歷史項目背景
- `hover:bg-blue-50` - 歷史項目 hover（推測）
- `hover:bg-blue-100` - 按鈕 hover（推測）

**文字色系**:
- `text-gray-800` - 標題
- `text-gray-500` - 時間、次要文字
- `text-gray-400` - 箭頭
- `text-purple-600` - 金額
- `text-blue-600` - 複製圖標
- `hover:text-red-600` - 刪除按鈕

**不一致點**:
1. **灰色階梯**: `gray-400`, `gray-500`, `gray-800`
2. **強調色不一致**: `purple-600`（金額） vs `blue-600`（圖標）

---

### 1.5 FavoritesList.tsx（常用貨幣）

**背景色系**:
- `bg-white` - 卡片背景
- `bg-gradient-to-r from-blue-50 to-purple-50` - 項目背景

**文字色系**:
- `text-yellow-500` - 星號圖標
- `text-gray-800` - 貨幣代碼
- `text-gray-600` - 貨幣名稱
- `text-green-500` - 上漲趨勢
- `text-red-500` - 下跌趨勢

**不一致點**:
1. **與 MultiConverter 不一致**:
   - 背景顏色：`from-blue-50 to-purple-50` vs `from-yellow-50 to-amber-50`
   - 應該都是收藏項目，但顏色不同

---

## 🔍 第二部分：色彩不一致分析

### 2.1 灰色系混亂（Gray vs Slate）

**問題**: 同時使用 `gray-*` 和 `slate-*`，沒有明確的使用場景區分

| 位置 | 使用的灰色 | 問題描述 |
|------|-----------|---------|
| RateWise 主頁標題 | `text-gray-800` | 主標題 |
| RateWise FAQ 標題 | `text-slate-800` | FAQ 區塊標題 |
| RateWise FAQ 內容 | `text-slate-600` | FAQ 內容文字 |
| RateWise 副標題 | `text-gray-600` | 說明文字 |
| SingleConverter 標籤 | `text-gray-700` | 表單標籤 |
| SingleConverter 次要資訊 | `text-slate-600` | 匯率卡片次要資訊 |
| MultiConverter 標籤 | `text-gray-700` | 表單標籤 |
| MultiConverter 貨幣代碼 | `text-gray-800` | 貨幣代碼 |
| ConversionHistory 標題 | `text-gray-800` | 卡片標題 |
| FavoritesList 貨幣代碼 | `text-gray-800` | 貨幣代碼 |

**影響**:
- 視覺不統一：`gray` 和 `slate` 色調略有差異
- 維護困難：不知道該用哪一個
- 語義混亂：無法從命名推斷用途

**應該的做法**:
- 統一使用 `neutral-*` 作為主要灰色系（Design Token）
- 明確定義：`neutral-900`（標題）、`neutral-700`（內文）、`neutral-500`（次要文字）

---

### 2.2 藍色系散亂（Blue vs Indigo）

**問題**: 藍色有 `blue-*` 和 `indigo-*` 兩個色系，使用場景不明確

| 位置 | 使用的藍色 | 問題描述 |
|------|-----------|---------|
| RateWise 主背景 | `from-blue-50 via-indigo-50 to-purple-50` | 三種藍色混用 |
| RateWise 模式切換器 | `from-blue-50 to-purple-50` | 藍色梯度 |
| RateWise FAQ 圖標背景 | `bg-indigo-100` | 使用 indigo |
| RateWise FAQ 連結 | `text-indigo-600` | 使用 indigo |
| SingleConverter 快速金額 | `hover:bg-blue-100` | 使用 blue |
| SingleConverter 輸入框 focus | `focus:border-blue-500` | 使用 blue |
| SingleConverter 匯率卡片 | `from-blue-100 to-purple-100` | 使用 blue |
| SingleConverter 交換按鈕 | `from-blue-500 to-purple-500` | 使用 blue |
| MultiConverter 快速金額 | `hover:bg-blue-100` | 使用 blue |
| ConversionHistory 圖標 | `text-blue-600` | 使用 blue |
| FavoritesList 項目背景 | `from-blue-50 to-purple-50` | 使用 blue |

**影響**:
- 品牌色不統一：有時 blue 有時 indigo
- 設計語言混亂：無法形成統一的視覺系統

**應該的做法**:
- 統一使用 `primary-*` 作為主色調（Design Token）
- `primary-500`（主按鈕）、`primary-100`（淺背景）、`primary-600`（hover）

---

### 2.3 紫色系混用（Purple vs Violet）

**問題**: 紫色有 `purple-*` 和 `violet-*` 兩個色系，使用場景重疊

| 位置 | 使用的紫色 | 問題描述 |
|------|-----------|---------|
| RateWise 品牌標題梯度 | `to-purple-600` | 品牌色使用 purple |
| RateWise 主背景梯度 | `to-purple-50` | 背景使用 purple |
| SingleConverter 計算機圖標 | `text-violet-600`, `hover:bg-violet-50` | 使用 violet |
| SingleConverter 結果輸入框 | `bg-purple-50`, `border-purple-200` | 使用 purple |
| SingleConverter 結果快速金額 | `bg-purple-100`, `hover:bg-purple-200` | 使用 purple |
| SingleConverter 結果計算機圖標 | `text-purple-600`, `hover:text-purple-700` | 使用 purple |
| ConversionHistory 金額 | `text-purple-600` | 使用 purple |
| FavoritesList 項目背景梯度 | `to-purple-50` | 使用 purple |

**影響**:
- 同一組件內混用：SingleConverter 同時有 `violet-600` 和 `purple-600`
- 色調差異：violet 偏藍紫，purple 偏紅紫
- 維護困難：改一個顏色要找兩個系列

**應該的做法**:
- 統一使用 `primary-*` 或 `accent-*`（取決於語義）
- 明確定義：`accent-500`（次要按鈕）、`accent-50`（淺背景）

---

### 2.4 收藏功能顏色不一致

**問題**: FavoritesList 和 MultiConverter 的收藏項目使用不同背景色

| 組件 | 背景色 | 星號顏色 |
|------|--------|---------|
| FavoritesList | `from-blue-50 to-purple-50` | `text-yellow-500` |
| MultiConverter（推測） | `from-yellow-50 to-amber-50` | `text-yellow-500` |

**影響**:
- 同一功能，不同視覺
- 用戶困惑：為什麼常用貨幣在不同地方顏色不同？

**應該的做法**:
- 統一使用 `warning-*` 系列（黃色系）
- `warning-50`（背景）、`warning-500`（圖標）

---

### 2.5 狀態顏色不一致

**問題**: 成功、錯誤、警告狀態顏色散佈在各處

| 狀態 | 位置 | 使用的顏色 |
|------|------|-----------|
| 錯誤 | RateWise 錯誤頁面 | `bg-red-50`, `text-red-500`, `bg-red-500` |
| 錯誤 | SingleConverter 錯誤訊息 | `text-red-500` |
| 刪除 | ConversionHistory 刪除按鈕 | `hover:text-red-600` |
| 下跌 | FavoritesList 下跌趨勢 | `text-red-500` |
| 上漲 | FavoritesList 上漲趨勢 | `text-green-500` |

**影響**:
- 紅色深淺不一：`red-500` vs `red-600`
- 綠色只在一處使用，缺乏系統性

**應該的做法**:
- `danger-500`（錯誤、刪除）
- `success-500`（成功、上漲）
- `warning-500`（警告）

---

## 📝 第三部分：詳細不一致點列表

### 3.1 標題文字顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| RateWise.tsx | 195 | 錯誤標題 | `text-gray-800` | 使用 gray |
| RateWise.tsx | 369 | FAQ 標題 | `text-slate-800` | 使用 slate |
| ConversionHistory.tsx | 65 | 卡片標題 | `text-gray-800` | 使用 gray |
| FavoritesList.tsx | 17 | 卡片標題 | `text-gray-800` | 使用 gray |

**為什麼不一致**: FAQ 區塊使用 `slate-*`，但其他標題使用 `gray-*`，沒有明確的語義區分。

---

### 3.2 副標題/說明文字顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| RateWise.tsx | 196 | 錯誤說明 | `text-gray-600` | 使用 gray-600 |
| RateWise.tsx | 293 | 版本說明 | `text-gray-600` | 使用 gray-600 |
| RateWise.tsx | 370 | FAQ 摘要 | `text-slate-500` | 使用 slate-500 |
| RateWise.tsx | 382 | FAQ 內容 | `text-slate-600` | 使用 slate-600 |
| SingleConverter.tsx | 389 | 匯率次要資訊 | `text-slate-600` | 使用 slate-600 |
| MultiConverter.tsx | 151 | 提示文字 | `text-gray-500` | 使用 gray-500 |
| ConversionHistory.tsx | 93 | 時間文字 | `text-gray-500` | 使用 gray-500 |

**為什麼不一致**:
- FAQ 區塊刻意使用 `slate-*`
- 其他地方混用 `gray-500` 和 `gray-600`
- 無法從命名推斷深淺差異的語義

---

### 3.3 快速金額按鈕顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| SingleConverter.tsx | 326 | 來源快速金額 | `bg-gray-100 hover:bg-blue-100 active:bg-blue-200` | 預設灰、hover 藍 |
| SingleConverter.tsx | 569 | 結果快速金額 | `bg-purple-100 hover:bg-purple-200 active:bg-purple-300` | 預設紫、hover 深紫 |
| MultiConverter.tsx | 159 | 快速金額 | `bg-gray-100 hover:bg-blue-100` | 預設灰、hover 藍 |

**為什麼不一致**:
- SingleConverter 的「來源」和「結果」區塊使用不同顏色
- 「結果」區塊用紫色想強調，但造成視覺混亂
- MultiConverter 使用與「來源」一致的顏色，但沒有統一設計

---

### 3.4 計算機圖標顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| SingleConverter.tsx | 308 | 來源計算機圖標 | `text-violet-600 hover:text-violet-700 hover:bg-violet-50` | 使用 violet |
| SingleConverter.tsx | 549 | 結果計算機圖標 | `text-purple-600 hover:text-purple-700 hover:bg-purple-50` | 使用 purple |

**為什麼不一致**:
- 同一組件內的同一功能（計算機按鈕）使用兩種不同的紫色
- `violet` 和 `purple` 色調略有差異
- 應該統一使用一種紫色系

---

### 3.5 輸入框 focus 狀態顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| SingleConverter.tsx | 298 | 來源輸入框 | `focus:border-blue-500 focus:ring-blue-500/20` | 使用 blue |
| SingleConverter.tsx | 539 | 結果輸入框 | `focus:border-purple-500 focus:ring-purple-500/20` | 使用 purple |

**為什麼不一致**:
- 「來源」和「結果」區塊刻意用不同顏色區分
- 但這會導致視覺不統一
- 應該統一使用主色調（blue 或 purple），用其他方式區分（如邊框粗細、陰影）

---

### 3.6 主要行動按鈕顏色一致性（唯一的亮點）

| 檔案 | 行號 | 元素 | 顏色 | 一致性 |
|------|------|------|------|--------|
| SingleConverter.tsx | 579 | 加入歷史按鈕 | `from-blue-600 to-purple-600 hover:from-blue-700 hover:to-purple-700` | ✅ 一致 |
| SingleConverter.tsx | 441 | 交換按鈕 | `from-blue-500 to-purple-500 hover:from-blue-600 hover:to-purple-600` | ✅ 類似（但深淺不同） |
| RateWise.tsx | 286 | 品牌標題 | `from-blue-600 to-purple-600` | ✅ 一致 |

**為什麼一致**: 藍紫梯度是品牌色，主要行動按鈕保持一致。但交換按鈕使用 `500` 系列，主按鈕使用 `600` 系列，仍有細微差異。

---

### 3.7 收藏項目背景顏色不一致

| 檔案 | 行號 | 元素 | 顏色 | 問題 |
|------|------|------|------|------|
| FavoritesList.tsx | 23 | 收藏項目 | `from-blue-50 to-purple-50` | 藍紫梯度 |
| MultiConverter.tsx | 推測 | 收藏項目 | `from-yellow-50 to-amber-50` | 黃色梯度 |

**為什麼不一致**:
- MultiConverter 想用黃色強調「收藏」功能
- FavoritesList 使用品牌色（藍紫梯度）
- 同一功能，不同視覺，用戶困惑

---

## 🎨 第四部分：色彩族群分析

### 4.1 Gray/Slate 系（灰色系統混亂）

**使用統計**:
- `gray-100`: 3 次（貨幣選擇器、快速金額按鈕）
- `gray-200`: 1 次（輸入框邊框）
- `gray-300`: 1 次（未收藏星號）
- `gray-400`: 1 次（歷史箭頭）
- `gray-500`: 5 次（提示文字、時間）
- `gray-600`: 7 次（說明文字、次要內容）
- `gray-700`: 3 次（表單標籤）
- `gray-800`: 5 次（標題、貨幣代碼）
- `slate-50`: 1 次（FAQ 背景梯度）
- `slate-100`: 1 次（FAQ 邊框）
- `slate-500`: 1 次（FAQ 摘要）
- `slate-600`: 3 次（FAQ 內容、匯率次要資訊）
- `slate-800`: 2 次（FAQ 標題）
- `slate-900`: 1 次（Footer 背景）

**問題**:
1. **命名重疊**: `gray-800` 和 `slate-800` 都用作標題
2. **色調差異**: `gray` 偏暖，`slate` 偏冷，視覺不統一
3. **無語義**: 無法從 `gray` 或 `slate` 推斷用途
4. **階梯不完整**: `gray` 階梯較完整，`slate` 只用幾個特定數字

**應該的做法**:
```typescript
const neutralColors = {
  50: '#fafafa',   // 淺背景
  100: '#f5f5f5',  // 按鈕背景
  200: '#e5e5e5',  // 邊框
  300: '#d4d4d4',  // 禁用文字
  400: '#a3a3a3',  // 次要圖標
  500: '#737373',  // 提示文字
  600: '#525252',  // 說明文字
  700: '#404040',  // 表單標籤
  800: '#262626',  // 標題
  900: '#171717',  // 深色背景
};
```

---

### 4.2 Blue 系（藍色使用散亂）

**使用統計**:
- `blue-50`: 5 次（背景梯度、模式切換器）
- `blue-100`: 1 次（匯率卡片、快速金額 hover）
- `blue-200`: 1 次（快速金額 active）
- `blue-400`: 1 次（交換按鈕梯度）
- `blue-500`: 3 次（交換按鈕、focus 邊框、focus ring）
- `blue-600`: 4 次（品牌標題、主按鈕）
- `blue-700`: 1 次（主按鈕 hover）
- `blue-900`: 1 次（Footer 背景）
- `indigo-50`: 1 次（主背景梯度）
- `indigo-100`: 1 次（FAQ 圖標背景）
- `indigo-600`: 2 次（FAQ 連結）
- `indigo-700`: 1 次（FAQ 連結 hover）
- `indigo-900`: 1 次（Footer 背景）

**問題**:
1. **Blue vs Indigo**: 無明確使用場景區分
2. **深淺跳躍**: `blue-50` → `blue-500` 之間跨度大
3. **Footer 混用**: `blue-900` 和 `indigo-900` 同時出現

**應該的做法**:
```typescript
const primaryColors = {
  50: '#eff6ff',   // from-blue-50
  100: '#dbeafe',  // from-blue-100
  200: '#bfdbfe',
  300: '#93c5fd',
  400: '#60a5fa',  // via-blue-400
  500: '#3b82f6',  // from-blue-500
  600: '#2563eb',  // from-blue-600
  700: '#1d4ed8',  // hover:from-blue-700
  800: '#1e40af',
  900: '#1e3a8a',
};
```

---

### 4.3 Purple 系（紫色使用散亂）

**使用統計**:
- `purple-50`: 5 次（背景梯度、結果輸入框背景）
- `purple-100`: 3 次（匯率卡片、結果快速金額按鈕）
- `purple-200`: 1 次（結果輸入框邊框、快速金額 hover）
- `purple-300`: 1 次（快速金額 active）
- `purple-400`: 1 次（交換按鈕梯度 via）
- `purple-500`: 3 次（交換按鈕、focus 邊框、focus ring）
- `purple-600`: 6 次（品牌標題、主按鈕、結果計算機圖標、歷史金額）
- `purple-700`: 1 次（主按鈕 hover、結果計算機圖標 hover）
- `violet-50`: 1 次（來源計算機圖標 hover 背景）
- `violet-600`: 1 次（來源計算機圖標）
- `violet-700`: 1 次（來源計算機圖標 hover）

**問題**:
1. **Purple vs Violet**: 同一組件內混用（SingleConverter）
2. **色調差異**: `violet` 偏藍紫，`purple` 偏紅紫
3. **語義混亂**: 無法推斷何時用 `purple`、何時用 `violet`

**應該的做法**:
```typescript
const accentColors = {
  50: '#faf5ff',   // to-purple-50
  100: '#f3e8ff',  // to-purple-100
  200: '#e9d5ff',  // border-purple-200
  300: '#d8b4fe',
  400: '#c084fc',  // via-purple-400
  500: '#a855f7',  // to-purple-500
  600: '#9333ea',  // to-purple-600
  700: '#7e22ce',  // hover:to-purple-700
  800: '#6b21a8',
  900: '#581c87',
};
```

---

### 4.4 其他色系

**紅色系（錯誤/刪除/下跌）**:
- `red-50`: 1 次（錯誤背景）
- `red-500`: 3 次（錯誤圖標、錯誤訊息、下跌趨勢）
- `red-600`: 2 次（錯誤按鈕 hover、刪除按鈕 hover）

**綠色系（成功/上漲）**:
- `green-500`: 1 次（上漲趨勢）

**黃色系（收藏/警告）**:
- `yellow-50`: 1 次（收藏背景梯度）
- `yellow-500`: 2 次（收藏星號、已收藏）
- `amber-50`: 1 次（收藏背景梯度）

**問題**:
1. **使用頻率低**: 紅色系相對一致，但綠色、黃色使用過少
2. **語義不明確**: `yellow` vs `amber` 何時使用？

**應該的做法**:
```typescript
const semanticColors = {
  danger: {
    50: '#fef2f2',    // bg-red-50
    500: '#ef4444',   // text-red-500
    600: '#dc2626',   // hover:bg-red-600
  },
  success: {
    50: '#f0fdf4',
    500: '#22c55e',   // text-green-500
    600: '#16a34a',
  },
  warning: {
    50: '#fffbeb',    // from-yellow-50
    500: '#eab308',   // text-yellow-500
    600: '#ca8a04',
  },
};
```

---

## ⚠️ 第五部分：為什麼需要 SSOT

### 5.1 設計不一致的影響

**1. 品牌認知混亂**:
- 用戶無法形成統一的品牌印象
- 藍色有時是 `blue`，有時是 `indigo`
- 灰色有時是 `gray`，有時是 `slate`

**2. 視覺層級混亂**:
- 無法從顏色推斷元素的重要性
- 標題有時 `gray-800`，有時 `slate-800`
- 說明文字有時 `gray-600`，有時 `slate-600`，有時 `gray-500`

**3. 互動反饋不一致**:
- 快速金額按鈕：來源區（灰→藍），結果區（紫→深紫）
- 計算機圖標：來源區（violet），結果區（purple）
- 輸入框 focus：來源區（blue），結果區（purple）

---

### 5.2 維護成本的影響

**1. 改色困難**:
- 需要搜尋多個檔案
- 需要手動替換多個顏色類
- 容易漏改或改錯

**範例**: 如果要統一所有「標題」顏色：
```bash
# 需要替換：
text-gray-800   # RateWise、ConversionHistory、FavoritesList
text-slate-800  # RateWise FAQ

# 可能遺漏：
text-gray-700   # 某些組件的標題可能用這個
```

**2. 新增組件時無所適從**:
- 不知道該用 `gray` 還是 `slate`
- 不知道該用 `blue` 還是 `indigo`
- 不知道該用 `purple` 還是 `violet`
- 導致不一致性持續擴大

**3. 設計稿與實作脫節**:
- 設計師：「這個用品牌紫」
- 開發者：「用 `purple-600` 還是 `violet-600`？」
- 最終：兩個人用了不同的紫色

---

### 5.3 可訪問性（Accessibility）的影響

**1. 對比度不一致**:
- `gray-600` vs `slate-600` 的對比度略有差異
- 可能導致某些文字無法通過 WCAG AA 標準

**2. 色盲用戶困擾**:
- 藍色系混用（blue, indigo）對色盲用戶難以區分
- 紫色系混用（purple, violet）同樣問題

**3. 暗色模式適配困難**:
- 顏色類散佈在各處，無法統一切換
- 需要逐一檢查每個組件的顏色

---

### 5.4 SSOT 的解決方案

**1. 語義化命名**:
```typescript
// ❌ 舊方式：硬編碼顏色
<h1 className="text-gray-800">標題</h1>
<p className="text-slate-600">內容</p>

// ✅ 新方式：語義化 token
<h1 className="text-neutral-900">標題</h1>
<p className="text-neutral-700">內容</p>
```

**2. 單一真實來源**:
```typescript
// src/config/design-tokens.ts
export const colors = {
  neutral: { /* ... */ },  // 灰色系統一在這裡定義
  primary: { /* ... */ },  // 藍色系統一在這裡定義
  accent: { /* ... */ },   // 紫色統一在這裡定義
  danger: { /* ... */ },
  success: { /* ... */ },
  warning: { /* ... */ },
};
```

**3. 工具函數封裝**:
```typescript
// src/utils/classnames.ts
export function getButtonClasses(variant: 'primary' | 'secondary') {
  return cn(
    'px-4 py-2 rounded-lg font-semibold transition',
    variant === 'primary' && 'bg-primary-500 hover:bg-primary-600 text-white',
    variant === 'secondary' && 'bg-neutral-100 hover:bg-neutral-200 text-neutral-900',
  );
}
```

**4. 測試保護**:
```typescript
// 確保所有組件使用語義化 token
it('should use semantic tokens instead of hardcoded colors', () => {
  const { container } = render(<MyComponent />);

  // ❌ 不應該出現
  expect(container.innerHTML).not.toContain('text-gray-800');
  expect(container.innerHTML).not.toContain('text-slate-800');

  // ✅ 應該使用
  expect(container.innerHTML).toContain('text-neutral-900');
});
```

---

## 📊 總結

### 不一致統計

| 問題類型 | 數量 | 嚴重性 |
|---------|------|--------|
| 灰色命名混亂（gray vs slate） | 14 種用法 | 🔴 高 |
| 藍色命名混亂（blue vs indigo） | 11 種用法 | 🔴 高 |
| 紫色命名混亂（purple vs violet） | 10 種用法 | 🔴 高 |
| 快速金額按鈕顏色不一致 | 3 處 | 🟡 中 |
| 計算機圖標顏色不一致 | 2 處 | 🟡 中 |
| 收藏項目背景不一致 | 2 處 | 🟡 中 |
| 輸入框 focus 狀態不一致 | 2 處 | 🟢 低 |

### SSOT 解決的核心問題

1. **統一命名**: `neutral`, `primary`, `accent`, `danger`, `success`, `warning`
2. **語義明確**: 從 token 名稱就能推斷用途
3. **單一來源**: 所有顏色定義在 `design-tokens.ts`
4. **易於維護**: 改一個地方，全站統一更新
5. **可測試**: 測試可以驗證組件使用正確的 token
6. **可訪問性**: 統一管理對比度，確保符合 WCAG 標準

---

**報告生成時間**: 2026-01-14
**分析者**: Claude Code
**版本**: v1.0
