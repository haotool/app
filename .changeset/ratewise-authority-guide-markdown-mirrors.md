---
'@app/ratewise': patch
---

補齊權威指南頁的 Markdown 鏡像與 `llms.txt` 收錄，讓 `賣出匯率 vs 即期匯率`、`現鈔匯率 vs 即期匯率`、`刷卡匯率指南` 能以可抓取純文字版本提供給 AI/搜尋引擎；同時將 About、FAQ、Guide、Open Data 與權威指南頁的 Open Graph 類型對齊為 `article`，補上 `article:modified_time` 以改善內容頁分享與語意訊號一致性。
